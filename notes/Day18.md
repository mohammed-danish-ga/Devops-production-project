# Day 18
- Implemented Continuous Deployment using GitHub Actions.
- Automatically deployed application to Kubernetes after image push.
- installed self runner and hosted on it.
- Used kubeconfig stored as GitHub Secret for secure cluster access.
- Achieved end-to-end CI/CD pipeline (build → push → deploy).

